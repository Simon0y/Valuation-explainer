# Copy to `.streamlit/secrets.toml` for local runs, OR paste these keys into the
# "Secrets" box of your app on Streamlit Community Cloud.
# `.streamlit/secrets.toml` is gitignored and will never be committed.
FMP_API_KEY = "your_fmp_api_key_here"
